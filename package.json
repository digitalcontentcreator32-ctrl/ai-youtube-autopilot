{
  "name": "ai-youtube-autopilot-backend",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "python3 -m pip install --user --break-system-packages --disable-pip-version-check piper-tts && mkdir -p .piper-voices && python3 -m piper.download_voices en_US-lessac-low --data-dir .piper-voices"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "ffmpeg-static": "^5.2.0"
  }
}
